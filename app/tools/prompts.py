"""
工具调用模块 - 提示词模板（gen_sql / gen_steps / gen_checklist / explain_doc）
硬约束同规划器：Oracle 语法、只读 SELECT、隐私红线。
"""

# ----------------------------------------------------------------------
# gen_sql：自然语言 → 只读 SQL
# ----------------------------------------------------------------------
GEN_SQL_PROMPT = """你是医院信息科 Oracle 运维专家。根据用户需求编写一条只读查询 SQL。

【需求】
{requirement}

【相关表结构（只能使用以下表/字段，若无匹配表请如实说明）】
{schema_context}

【相关业务知识】
{knowledge}

【硬约束】
- 一律 Oracle 语法：TO_CHAR/NVL/DECODE/ROWNUM/FETCH FIRST/dual/SYSDATE；禁止 MySQL 语法（LIMIT/IFNULL/DATE_FORMAT/NOW()/反引号等）；
- 单条只读 SELECT/WITH；禁止任何写操作与伪写操作；
- 加 FETCH FIRST 30 ROWS ONLY 控制行数；
- 不查询患者敏感明细（姓名/证件/联系方式），优先聚合口径。

只输出 JSON：{{"sql": "...", "purpose": "查询目的说明", "notes": "表名/字段待核对说明（可选）"}}"""

# SQL 校验失败后的重试提示
GEN_SQL_RETRY = """你生成的 SQL 未通过只读校验，原因：{reason}
原 SQL：{sql}
请重新输出 JSON（格式不变），SQL 必须是单条 Oracle 只读 SELECT/WITH 查询。"""

# ----------------------------------------------------------------------
# gen_steps：排障步骤
# ----------------------------------------------------------------------
GEN_STEPS_PROMPT = """你是医院信息科资深运维专家。针对下面的问题，给出结构化排查步骤。

【问题描述】
{problem}

【知识库检索结果】
{rag_context}

要求：
- 3~6 步，按排查优先级排序（先易后难、先无害后深入）；
- 每步给出：步骤名、具体做法（含命令或 Oracle 只读 SQL 示例）、预期结果判断；
- SQL 一律 Oracle 只读语法；
- 涉及生产变更的操作要标注风险提示。

只输出 JSON：{{"steps": [{{"title": "步骤名", "detail": "具体做法", "command": "命令或SQL（可空串）", "risk": "风险提示（可空串）"}}]}}"""

# ----------------------------------------------------------------------
# gen_checklist：巡检清单
# ----------------------------------------------------------------------
GEN_CHECKLIST_PROMPT = """你是医院信息科运维专家。生成一份日常巡检清单。

【巡检范围/主题】
{topic}

【知识库检索结果】
{rag_context}

要求：
- 6~10 项，覆盖数据库、表空间、会话/锁、接口积压、备份、告警等（按主题裁剪）；
- 每项给出：巡检项、检查方法（Oracle 只读 SQL 或命令）、判定标准（正常阈值）；
- SQL 一律 Oracle 只读语法。

只输出 JSON：{{"items": [{{"item": "巡检项", "method": "检查方法（SQL/命令）", "standard": "判定标准"}}]}}"""

# ----------------------------------------------------------------------
# explain_doc：引用式解释
# ----------------------------------------------------------------------
EXPLAIN_DOC_PROMPT = """你是医院信息科运维专家。请基于知识库命中的内容解释用户问题，不得编造未引用内容。

【用户问题】
{question}

【知识库命中内容】
{rag_context}

要求：
- 用 markdown 输出解释；
- 关键结论后用「（来源：文档标题）」标注引用来源；
- 命中内容不足时明确说明"知识库暂无相关资料，以下为通用经验"。

直接输出解释正文（不用 JSON）。"""
